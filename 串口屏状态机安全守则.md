# 串口显示状态机安全守则

## 1. 目的与边界

本守则适用于所有通过 UART、USB 虚拟串口、RS-485 或类似低速链路向显示终端发送波形、频谱、日志或测量结果的嵌入式系统。

核心原则只有一句：**显示通信是可延迟、可丢弃、可恢复的低优先级业务；核心业务绝不等待显示通信。**

终端未连接、终端复位、协议错误、发送拥塞、DMA 异常或接收端停止处理时，控制、采集、保护、通信协议和主状态机都必须继续运行。

## 2. 强制规则

1. 核心状态机、实时控制环、采样中断、DMA 回调和定时器中断中，禁止使用阻塞发送。
2. 禁止无限等待发送完成、等待寄存器标志、发送后延时，以及“失败后立即循环重试”。
3. 发送完成/错误回调只置标志、记录错误和时间戳；不得在回调中格式化大文本、处理波形或递归发送长帧。
4. 显示发送必须有独立状态机，生产者只提交“最新显示快照”，不关心终端是否已经显示。
5. 终端带宽不足时丢弃旧帧，保留最新帧；禁止无限增长的发送队列。
6. DMA 正在读取的内存绝不允许改写；每个缓冲区必须有明确所有者。
7. 单次发送限制长度，长波形或长文本必须分片；每次状态机调用只推进一个有限步骤。
8. 每笔发送都必须设置超时。超时后退出忙状态、记录故障、退避重试，不能永久卡住。
9. 刷新率必须受限，并按实际链路吞吐量预算；关键告警和数值摘要优先于波形刷新。
10. 中断与前台共享的状态使用 `volatile`；多字段状态切换使用极短临界区或原子操作。

## 3. 推荐状态机

```text
生产者（控制、采集、算法、协议）
    └─ 提交最新显示快照：常数时间，不发送
             ↓
显示发送状态机
    IDLE     : 取走最新待发快照，准备一个短分片
    ACTIVE   : DMA/中断正在发送，只检查完成、错误或超时
    RECOVER  : 已中止故障传输，退避后回到 IDLE
```

关键约束：状态机每次被调用时必须在有限时间内返回。不能为了“把一整帧发完”而在一次调用中循环发送所有数据。

## 4. 缓冲区所有权

小型数值结果可使用“最新值槽”：生产者写入新值时直接覆盖旧值，并置 `pending` 标志。

波形、图像或频谱等大数据建议使用三缓冲所有权：

| 缓冲区 | 所有者 | 允许操作 |
| --- | --- | --- |
| build | 生产者 | 写入下一份显示快照 |
| pending | 显示状态机 | 等待序列化或发送 |
| active | UART/DMA | 只读，直到完成或中止 |

当 `pending` 已存在而新数据到来时，用新数据替换旧 `pending`，并增加丢帧计数。绝不覆盖 `active`。

## 5. 通用非阻塞模板

以下是平台无关的伪 C。`Transport_StartAsync()` 可以映射到 UART DMA、UART 中断、USB 端点发送等异步接口。

```c
typedef enum {
    DISPLAY_IDLE,
    DISPLAY_ACTIVE,
    DISPLAY_RECOVER,
} display_state_t;

static volatile display_state_t display_state;
static volatile bool display_tx_done;
static volatile uint32_t display_tx_error;
static volatile bool display_pending;
static uint32_t display_started_ms;
static uint32_t display_retry_after_ms;

#define DISPLAY_TX_TIMEOUT_MS  50U
#define DISPLAY_RETRY_DELAY_MS 200U

/* 异步发送完成/错误回调：只置标志。 */
void Display_OnTransportComplete(void)
{
    display_tx_done = true;
}

void Display_OnTransportError(uint32_t error_code)
{
    display_tx_error = error_code;
}

/* 前台周期调用：每次最多推进一个状态转换。 */
void Display_Process(uint32_t now_ms)
{
    if (display_state == DISPLAY_ACTIVE) {
        if (display_tx_done) {
            display_tx_done = false;
            display_state = DISPLAY_IDLE;
        } else if ((display_tx_error != 0U) ||
                   ((uint32_t)(now_ms - display_started_ms) >
                    DISPLAY_TX_TIMEOUT_MS)) {
            Transport_AbortAsync();
            display_tx_error = 0U;
            display_retry_after_ms = now_ms + DISPLAY_RETRY_DELAY_MS;
            display_state = DISPLAY_RECOVER;
        }
        return;
    }

    if (display_state == DISPLAY_RECOVER) {
        if ((int32_t)(now_ms - display_retry_after_ms) >= 0) {
            display_state = DISPLAY_IDLE;
        }
        return;
    }

    if (!display_pending) {
        return;
    }

    /* 在短临界区内取得最新快照；不要在临界区格式化或发送。 */
    display_pending = false;
    BuildNextShortChunk();

    display_tx_done = false;
    display_tx_error = 0U;
    if (Transport_StartAsync() == TRANSPORT_OK) {
        display_started_ms = now_ms;
        display_state = DISPLAY_ACTIVE;
    } else {
        display_retry_after_ms = now_ms + DISPLAY_RETRY_DELAY_MS;
        display_state = DISPLAY_RECOVER;
    }
}
```

## 6. 带宽与刷新预算

异步不等于无限带宽。链路必须留余量。

以 UART 的 8N1 为例，每发送 1 字节约占 10 个串行位：

```text
有效字节率 ≈ 波特率 / 10
长期显示预算 ≤ 有效字节率的 50%～70%
单帧预算 = 长期显示预算 / 刷新率
```

例如 115200 baud 的理论有效字节率约为 11520 B/s。若显示预算取 50%、刷新率为 20 Hz，则单帧建议不超过约 288 B。

- 大波形先抽取或压缩为适合屏幕分辨率的点数。
- 页面不可见时停止发送图形帧。
- 数值摘要、告警和操作确认使用更高优先级。
- 新图形帧到来时，允许放弃尚未发送的旧图形帧。

## 7. 超时与故障恢复

- 记录发送超时数、硬件错误数、丢帧数、最后成功发送时间。
- 超时后必须释放软件忙状态和缓冲区所有权；确认底层传输已中止后才能复用缓冲区。
- 连续故障采用退避重试，例如 100 ms、200 ms、500 ms、1 s，避免故障终端占满 CPU。
- 终端长期离线时，显示模块应保持低负载，不影响看门狗喂狗、采样和控制。

## 8. 明确禁止的写法

```c
/* 禁止：接收端异常时可能永久阻塞。 */
Transport_SendBlocking(frame, length, WAIT_FOREVER);

/* 禁止：把每个采样点同步发送。 */
for (uint32_t i = 0; i < sample_count; ++i) {
    SendOneTextLine(wave[i]);
}

/* 禁止：在完成回调中处理长数据并递归启动下一长帧。 */
void OnTxComplete(void)
{
    FormatWholeWaveform();
    Transport_StartAsync();
}
```

## 9. 验收清单

1. 正常连接终端，确认核心任务周期、实时性和数据正确性。
2. 终端断电、复位、断线或停止接收，确认核心任务持续运行且状态机能超时恢复。
3. 人为提交超过链路带宽的显示请求，确认增加的是丢帧计数，而不是 RAM 队列、CPU 占用或控制延迟。
4. 发送中切换页面或更新波形，确认发送中缓冲区未被改写，无乱码、越界或 HardFault。
5. 在故障状态持续运行数分钟，确认系统不因显示链路而卡死、复位或失去实时性；终端恢复后能够自动继续显示。

## 10. 最终准则

显示模块是旁路消费者，不是系统时序的主人。任何时候，宁可丢一帧显示数据，也不能让终端通信阻塞核心业务。
