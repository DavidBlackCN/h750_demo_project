# 设备支持

| 角色 | 型号 | 验证状态 |
| --- | --- | --- |
| 示波器 | SDS2502X Plus 5.0.1.3.7R5 | USB 状态、通道、时基、边沿触发、控制和有界 BYTE 波形已验证 |
| 示波器 | 其他 SDS2000X Plus | 兼容但未实机验证 |
| 信号源 | SDG6032X-E 6.01.01.35R5B1 | USB 状态、四种基本波、High-Z、输出 OFF/ON、扫频和最终 OFF 已在 1 Vpp 验证 |
| 信号源 | 其他 SDG6000X/SDG6000X-E | 兼容但未实机验证 |

当前公开 MCP 提供 15 个工具，包括只读状态、受控写入、波形采集和完整回路。

## PNG 截图 TODO

当前 SDS 固件通过 USB-VISA 对 `PRIN? BMP/PNG` 无响应。不要宣称截图可用，也不要使用仪器文件系统绕过限制。待兼容命令和实机读取验证完成后再更新支持状态。

## 交通方式

仅支持 USB Device + USBTMC + NI-VISA。PID 可能随型号变化；以 `*IDN?`、型号和序列号作为最终身份依据，不硬编码 PID 选择设备。
