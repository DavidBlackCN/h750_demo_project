# 淘晶驰串口屏竞赛模板

本模板面向 `TJC1060X570_011C_I`（1024 x 600）及采用同一 USART HMI
指令集的淘晶驰串口屏。它将比赛程序分为“屏幕只发送固定命令、STM32保存状态和切页、业务层接收一个任务配置”三部分。这样更换题目时只需覆盖任务启动和参数校验，不必重做页面通信。

当前固件实现位于 `HDL/TJC_HMI.*` 与 `API/TJC_HMI_API.*`，已支持：

- 自定义命令帧 `5A A5 <cmd> FF FF FF`；
- `get keypad.t_value.txt` 返回的 `70 <ASCII> FF FF FF` 文本帧；
- 屏幕返回的数值帧 `71 <uint32 little-endian> FF FF FF`；
- 上电 `88 FF FF FF`、标准触摸 `65 ... FF FF FF` 帧。

固定标题可在 HMI 工程中使用中文；STM32动态文本建议暂用 ASCII，避免字库和编码差异影响比赛联调。

## 1. 接线和工程全局设置

| 串口屏 | STM32H750 | 说明 |
| --- | --- | --- |
| RX | `PB10 / USART3_TX` | 交叉连接 |
| TX | `PB11 / USART3_RX` | 交叉连接；首次建议串联 1 kOhm |
| GND | GND | 屏幕、开发板、信号源必须共地 |
| 5V | 独立合适电源 | 不要由开发板的 3.3 V 供电 |

屏幕必须处于 TTL 串口模式，通信参数为 `115200, 8N1, no parity, no flow control`。在 USART HMI 的 `program.s` 写入以下配置，最后一行必须为页面跳转：

```text
bauds=115200
bkcmd=0
recmod=0
page home
```

不要启用按钮的“发送键值”，否则会附带标准 `0x65` 触摸帧。下文全部事件均写在按钮的“弹起事件”。`printh` 中的末尾 `FF FF FF` 是 STM32协议帧的一部分，必须保留。

## 2. 页面、控件和流程

必须创建以下五个页面，名称和动态控件名区分大小写：

```text
home -> menu -> control -> keypad
                    `-> result
```

### home

| 类型 | 名称 | 建议文字 |
| --- | --- | --- |
| 文本 | `t_title` | 信号系统测试平台 |
| 文本 | `t_status` | READY |
| 按钮 | `b_enter` | 开始演示 |
| 按钮 | `b_reset` | 重置程序 |

```text
// b_enter
printh 5A A5 01 FF FF FF

// b_reset
printh 5A A5 02 FF FF FF
```

`b_reset` 会调用 `NVIC_SystemReset()`；只有需要硬复位全部外设时才使用它。

### menu

| 类型 | 名称 | 建议文字 |
| --- | --- | --- |
| 按钮 | `b_task1` | 任务一 |
| 按钮 | `b_task2` | 任务二 |
| 按钮 | `b_task3` | 任务三 |
| 按钮 | `b_task4` | 任务四 |
| 按钮 | `b_task5` | 任务五 |
| 按钮 | `b_home` | 返回首页 |

```text
// b_task1 ... b_task5
printh 5A A5 10 FF FF FF
printh 5A A5 11 FF FF FF
printh 5A A5 12 FF FF FF
printh 5A A5 13 FF FF FF
printh 5A A5 14 FF FF FF

// b_home
printh 5A A5 33 FF FF FF
```

任务一、任务二被选择后直接进入 `result`；任务三、四、五先进入 `control`，由用户确认频率和 Vpp。当前模板仅记录任务选择和参数，并显示 `RESERVED`，不会启动任何外设、输出信号或业务程序。

### control

| 类型 | 名称 | 用途 |
| --- | --- | --- |
| 文本 | `t_task` | 当前任务，例如 `TASK 3` |
| 文本 | `t_freq` | 当前频率，例如 `1000 Hz` |
| 文本 | `t_vpp` | 当前幅度，例如 `1.0 Vpp` |
| 文本 | `t_hint` | `READY`、`VALUE UPDATED` 等 |
| 按钮 | `b_freq` | 修改频率 |
| 按钮 | `b_vpp` | 修改 Vpp |
| 按钮 | `b_start` | 启动任务 |
| 按钮 | `b_back` | 返回功能选择 |

```text
// b_freq
printh 5A A5 20 FF FF FF

// b_vpp
printh 5A A5 21 FF FF FF

// b_start
printh 5A A5 30 FF FF FF

// b_back
printh 5A A5 31 FF FF FF
```

### keypad

| 类型 | 名称 | 用途 |
| --- | --- | --- |
| 文本 | `t_key_title` | `INPUT FREQUENCY`、`INPUT VPP` 或错误提示 |
| 文本 | `t_value` | 当前输入；`txt_maxl=12` |
| 文本 | `t_unit` | `Hz` 或 `Vpp` |
| 按钮 | `b_0` 至 `b_9` | 数字输入 |
| 按钮 | `b_dot` | 小数点；频率输入时 MCU 自动隐藏 |
| 按钮 | `b_clear` | 清空输入 |
| 按钮 | `b_ok` | 确认输入 |
| 按钮 | `b_cancel` | 取消输入 |

每个数字按钮使用同一模式，例如：

```text
// b_0、b_1、...、b_9 的弹起事件
t_value.txt+="0"
t_value.txt+="1"
```

`b_dot`、`b_clear`、`b_ok`、`b_cancel` 的弹起事件：

```text
// b_dot
t_value.txt+="."

// b_clear
t_value.txt=""

// b_ok
printh 5A A5 22 FF FF FF

// b_cancel
printh 5A A5 23 FF FF FF
```

收到确认命令后，STM32下发 `get keypad.t_value.txt`。屏幕会返回字节码
`70 <ASCII 输入字符> FF FF FF`，驱动解析后检查格式和范围，成功则回到
`control`；失败会把 `t_key_title` 改为 `INVALID INPUT`。模板默认接受 `1` 到
`10000000` Hz 的整数频率，以及 `0.1` 到 `20.0` Vpp 且最多一位小数；实际题目应在业务层覆盖校验钩子。

### result

| 类型 | 名称 | 用途 |
| --- | --- | --- |
| 文本 | `t_status` | `RUNNING`、`ERROR`、`STOPPED` |
| 文本 | `t_task` | 当前任务 |
| 文本 | `t_freq` | 当前频率 |
| 文本 | `t_vpp` | 当前 Vpp |
| 文本 | `t_detail` | 业务摘要，例如测量值或错误原因 |
| 按钮 | `b_again` | 回参数页 |
| 按钮 | `b_menu` | 回任务菜单 |
| 按钮 | `b_home` | 停止并回首页 |
| 按钮 | `b_stop` | 停止当前任务 |

```text
// b_again
printh 5A A5 32 FF FF FF

// b_menu
printh 5A A5 31 FF FF FF

// b_home
printh 5A A5 33 FF FF FF

// b_stop
printh 5A A5 34 FF FF FF
```

## 3. 串口字节码总表

每个屏幕按钮发出的完整 UART 帧均为 6 字节：`5A A5 <命令字> FF FF FF`。

| 命令字 | 完整字节码 | MCU动作 |
| --- | --- | --- |
| `01` | `5A A5 01 FF FF FF` | home 到 menu |
| `02` | `5A A5 02 FF FF FF` | MCU硬复位 |
| `10` 至 `14` | `5A A5 10..14 FF FF FF` | 选择任务 1 至 5 |
| `20` | `5A A5 20 FF FF FF` | 编辑频率 |
| `21` | `5A A5 21 FF FF FF` | 编辑 Vpp |
| `22` | `5A A5 22 FF FF FF` | 读取键盘文本 |
| `23` | `5A A5 23 FF FF FF` | 取消键盘输入 |
| `30` | `5A A5 30 FF FF FF` | 确认任务 3 至 5；当前仅置为保留状态 |
| `31` | `5A A5 31 FF FF FF` | 返回菜单 |
| `32` | `5A A5 32 FF FF FF` | 返回参数页 |
| `33` | `5A A5 33 FF FF FF` | 停止并返回首页 |
| `34` | `5A A5 34 FF FF FF` | 停止当前任务 |

MCU到屏幕的所有标准指令以三个 `FF` 结束。例如：

```text
page result FF FF FF
control.t_freq.txt="1000 Hz" FF FF FF
result.t_detail.txt="FREQ=999.8HZ" FF FF FF
get keypad.t_value.txt FF FF FF
```

## 4. 固件接入和题目适配

串口屏不是当前 AD9959/测频主任务的已启用外设。切换到串口屏任务时，在 `main.c` 初始化阶段加入：

```c
MX_USART3_UART_Init();
if (TJC_HMI_API_Init(&huart3) != HAL_OK)
{
    Error_Handler();
}
```

并在 `while (1)` 中持续调用：

```c
TJC_HMI_API_Process();
```

不要与占用 USART3/PB10/PB11 的其他任务同时启用。`TJC_HMI_API_GetTaskConfig()` 可读取当前的 `task_id`、`frequency_hz`、`vpp_mvpp`，`TJC_HMI_API_GetState()` 可读取 `IDLE`、`SELECTED`、`CONFIGURING` 或 `RESERVED`。当前模板刻意不调用 `TJC_HMI_API_OnTaskStart()` 和 `TJC_HMI_API_OnTaskStop()`，因此按钮不会操作任何题目硬件。

后续真正做题时，可以在应用层同名实现这两个弱函数，并在业务主状态机准备好、资源确认无冲突后，再将 `TJC_HMI_API_ReserveTask()` 接到对应的任务启动逻辑；`TJC_HMI_API_OnTaskStart()` 的输入 `TJC_HMI_API_TaskConfig` 包含 `task_id`、`frequency_hz`、`vpp_mvpp`。

例如，题目业务可在 `API/Your_Task_API.c` 中覆盖：

```c
HAL_StatusTypeDef TJC_HMI_API_OnTaskStart(const TJC_HMI_API_TaskConfig *config)
{
    switch (config->task_id)
    {
    case 1U: return Your_Task1_Start();
    case 2U: return Your_Task2_Start();
    default: return Your_Output_Start(config->frequency_hz, config->vpp_mvpp);
    }
}

void TJC_HMI_API_OnTaskStop(void)
{
    Your_Output_Stop();
}
```

后续测量完成或出现业务错误时，可调用 `TJC_HMI_API_Report("DONE", "FREQ=999.8HZ")` 或
`TJC_HMI_API_Report("ERROR", "ADC TIMEOUT")` 更新结果页。现阶段只会通过 USART1 输出一条可供电脑验收的日志：

```text
HMI task=3 freq=1000Hz vpp=1000mVpp status=RESERVED detail=TASK NOT CONNECTED
```

实际题目参数范围应覆盖 `TJC_HMI_API_ValidateFrequencyHz()` 和
`TJC_HMI_API_ValidateVppMvpp()` 定义；返回非零表示接受输入。不要仅依赖屏幕端限制，所有输入必须由 STM32再次校验。

## 5. 下载和最小联调

1. 在 USART HMI 编译工程，先在模拟器检查五个页面和数字键盘。
2. 下载生成的 TFT 文件，断电完成 USART3 交叉接线并确认共地后上电。
3. 切换固件主任务以初始化 USART3 和 `TJC_HMI_API`，再烧录。
4. 按 `home -> menu -> task3 -> control -> keypad -> result` 验证页面和参数回传。
5. 电脑连接 USART1 调试口，确认任务一/二的选择、或任务三至五的确认操作仅打印一条 `HMI task=... status=RESERVED` 日志。

出现“有画面、无响应”时，依次检查屏 TX/RX、TTL模式、共地、波特率、按钮事件是否为弹起事件、`printh`的六个字节是否完整，以及“发送键值”是否关闭。动态文本不更新时检查页面和控件名、`txt_maxl`、`bkcmd=0`，并先以 ASCII 文本联调。

## 6. 当前双 DDS 并行任务

当前 `main.c` 启动 AD9833 的 1 kHz、0 deg 正弦和 AD9910 模块直接端的 100 kHz、300 mVpp 正弦，然后初始化并持续运行 `TJC_HMI_API` 页面状态机。两路 DDS 只在启动阶段写寄存器，后续由外部芯片持续输出；串口屏事件只读写 USART3 和页面状态，因此不会打断 DDS 输出。

串口屏保持 `115200, 8N1`、TTL 模式。屏幕上电完成后会发送 ready 帧，固件收到后执行 `page home`。点击任务和参数按键仍遵循本文的 `printh 5A A5 <cmd> FF FF FF` 协议，但当前只切换模板页面和 `RESERVED` 状态，不重新配置 DDS 或启动任何题目业务。

用示波器分别接 AD9833 和 AD9910 的模拟输出端验证频率、波形与幅度。AD9910 的 `300 mVpp` 为驱动的模块直接输出标称值，受模块系统时钟、输出网络和负载影响，不能代替实测验收。
