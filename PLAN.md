# G题“周期信号测量分析装置”题目分析与实施计划

> 日期：2026-07-29
> 当前边界：仅分析题目并形成计划；本轮不修改代码、不编译、不烧录、不声称完成实物验证。

## 1. 结论先行

主采集路线建议选择 **AD9226 + STM32H750**，STM32H750 片内 ADC 作为限时失败后的备选路线。

推荐的主数据链为：

`BNC/50Ω输入 -> 保护与宽带缓冲 -> 500 kHz通带低通 -> 两档增益 -> AD9226高速采样 -> H750 DCMI/DMA -> 数字低通与抽取 -> 测量/频谱/显示`

选择 AD9226 的关键原因不是“12 位比片内 ADC 位数高”，而是它可用较高采样率先完整采入 500 kHz 被测信号和 1 MHz 以上干扰，再在数字域进行低通与抽取，给抗混叠、干扰抑制和幅值拟合留下明显余量。

但现有 AD9226 链路目前只有编译证据，没有上板证据。因此实施时必须设置 1.5～2 小时的 go/no-go 门槛：如果高速时钟、DCMI 码序、静态码和 500 kHz 正弦采集不能按时通过，立即切换到片内 ADC，不继续无期限排查并口。

## 2. 题目硬约束

| 项目 | 输入条件 | 必须完成 |
| --- | --- | --- |
| 第1项 | `u=ua`，100～250 mVpp，全部频率分量在 10～200 kHz | 1/3 周期波形；Upp、Urms；基频；定性频谱；各频率分量幅值 |
| 第2项 | `u=ub`，50～250 mVpp，全部频率分量在 10～500 kHz | 同上 |
| 第3项 | `u=ub+uJ`，ub 同第2项；uJ=200 mVpp 单频，fJ≥1 MHz | 抑制 uJ 后测量并显示 ub 的全部上述结果 |

共同指标：

- 每一项从装置启动到完成不超过 2 s。
- Upp、真有效值 Urms 的绝对误差均不超过 5 mV。
- 基频绝对误差不超过 1 kHz。
- 各频率分量幅值绝对误差不超过 5 mV。
- 频率分辨率为 500 Hz。
- 被测周期信号由基波和 1 个或 2 个谐波组成；基波不一定是最大谱线。
- 频谱只显示正频率轴。
- 题中公式的 `U1、U3、U4` 是正弦项前的峰值系数，因此“频谱分量幅值”应显示峰值幅度，不应显示 RMS 幅度；时域的 Urms 则必须是真有效值。
- 单路 5 V 直流稳压电源供电。
- 显示屏不小于 6 英寸。
- 板上必须焊接 BNC 输入座；信号源输出阻抗和电缆阻抗为 50 Ω。

## 3. 真正的技术难点

### 3.1 小信号绝对幅值精度

最低输入只有 50 mVpp，而允许的绝对误差为 5 mV。只依赖 ADC 标称位数不够，必须同时处理：

- 50 Ω源与装置输入负载造成的幅值关系；
- 前端增益、偏置、频响和温漂；
- ADC 参考、电源噪声、零点、增益误差；
- 非相干采样造成的频谱泄漏和幅值误差；
- 最大值/最小值受噪声和离群点影响的问题。

装置内部设计目标应收紧到幅值误差不超过 3 mV，给现场信号源、线缆、温漂和重复性留余量。

### 3.2 500 kHz 通带与 1 MHz 阻带距离很近

第3项要求保留 500 kHz 以内信号，同时抑制从 1 MHz 开始的 200 mVpp 干扰，过渡带只有约一个倍频程。

若希望 200 mVpp 干扰残留低于 1 mVpp，模拟与数字滤波的合计抑制度应达到约 46 dB；工程目标取 **1 MHz 处合计不低于 50 dB**。模拟滤波还必须在 ADC 前承担防削顶和防高频混叠的任务，不能只依赖采样后的数字滤波。

### 3.3 基频不一定是最高谱线

不能简单把最大 FFT 峰当作基频。应先找出 2～3 根显著谱线，再搜索能使这些谱线接近整数倍的最小基频；随后在该基频及其整数倍上做精确幅值拟合。

### 3.4 稳定显示与 2 s 限时

采集和计算本身只需毫秒到百毫秒，主要风险是：

- 自动量程反复切换；
- 串口屏逐点命令过多；
- 直接显示原始波形造成触发漂移；
- 把 4096 个频谱点全部发送到屏幕。

显示数据只需要与屏幕像素数相匹配。波形应按估计基频重构为稳定的 1 周期或 3 周期；频谱只需要绘制经压缩后的正频率谱和 2～3 根主要谱线。

## 4. AD9226 与片内 ADC 对比

| 比较项 | AD9226 + H750 | H750 片内 ADC |
| --- | --- | --- |
| 官方能力 | 12 位、最高 65 MSPS；支持 3 V 数字输出驱动 | 3 个最高 16 位 ADC；器件页给出最高 3.6 MSPS，低分辨率模式可更高 |
| 本题建议工作点 | 8 MS/s 原始采样，数字低通后 4 倍抽取到 2 MS/s | 12 位、约 4 MS/s、8192 点原始帧，数字低通后 2 倍抽取到 2 MS/s、4096 点分析帧 |
| 500 Hz 频率分辨率 | 原始 16384 点采 2.048 ms，抽取后 4096 点、2 MS/s，FFT 间隔 488.28125 Hz | 原始 8192 点同样采 2.048 ms，抽取后 FFT 间隔为 488.28125 Hz |
| 500 kHz 每周期点数 | 原始约 16 点，抽取后约 4 点；幅值拟合可优先使用原始/滤波数据 | 约 8 点 |
| ≥1 MHz 干扰处理 | 可先采入，再数字低通/抽取；抗混叠余量较大 | 1 MHz 已靠近有效奈奎斯特区，仍可数字滤波，但高频干扰混叠风险更高 |
| 模拟前端 | 仍需要低通、增益和实际量程标定 | 同样需要低通、增益、偏置和 ADC 驱动 |
| 软件/硬件复杂度 | 12 位并口、时钟、DCMI、DMA、码序和缓存较复杂 | 引脚少，TIM 触发 DMA 更简单 |
| 当前工程基础 | DCMI/DMA、4096 点采集和 AD9226 分层已有，但仅编译通过；当前算法只适合约 1 kHz | 片内 ADC、DMA、4096 点 FFT、频率估计已有较多基础，部分 ADC 链路有实物经验 |
| 当前直接可用性 | 不可直接使用：当前为 1 MS/s，算法限定 800～1200 点/周期 | 不可直接使用：现有 ADC2 FFT 主要覆盖 1～100 kHz，当前采样率和搜索范围不足 |
| 主要比赛风险 | 并口高速连线、时钟质量、DCMI 位序和模块模拟量程未实测 | 高频端采样余量、片内噪声、ADC 驱动和干扰混叠余量较小 |

### 最终选择

- **冲击完整 100 分功能：选 AD9226 + H750。**
- **若 AD9226 高速采集在限时内未通过：切换 H750 片内 ADC，优先稳拿第1项和第2项，再用模拟低通争取第3项。**
- 不建议用 ADS8688 作为本题主 ADC：其最高吞吐率为 500 kSPS，无法直接覆盖 500 kHz 被测带宽。

## 5. 对已有模块的取舍

### 可重点复用

- `HDL/AD9226.*`
  - 可复用 12 位 DCMI、DMA、RAM_D2 和 D-Cache 处理框架。
  - 必须把 1 MS/s 改为题目需要的高速采样，并重新验证时钟、码序和丢样。
- `BLL/ADC2_FFT_BLL.*` 与 `FML/ADC2_FFT_FML.*`
  - 已有 4096 点无大表 FFT、峰值插值和通用 ADC 输入描述，可作为抽取后 2 MS/s 数据的算法基础。
  - 必须扩展到 10～500 kHz、多谱线、基频整数关系判定和精确幅值拟合。
- `FML/FFT_FML.*`、`BLL/FFT_BLL.*`
  - 可复用窗函数、谱峰和插值思路。
- `HDL/TJC_HMI.*`、`API/TJC_HMI_API.*`
  - 可复用串口屏收发、页面和按键状态机。
  - 当前题目业务仍为 `RESERVED`，且 115200 波特率是否满足稳定刷新需要实测。
- 现有 DMA、RAM_D2、D-Cache 和前后台分离经验。

### 可作为前端候选

- OPA695 / OPA690：支持单 5 V，带宽远高于本题，可用于宽带缓冲、固定增益和有源低通，但需要按单电源中点偏置设计并实测输出摆幅。
- ADG1412 或继电器模块：用于两档固定增益/输入终端切换；继电器路径更直观但切换慢，仍远小于 2 s。
- 钳位保护模块：用于 BNC 入口保护，但必须验证其结电容不会破坏 500 kHz 幅频响应。

### 不作为主链

- MAX262：官方中心/截止频率能力最高约 140 kHz，不能作为保留 500 kHz 通带的主低通。
- AD603：带宽和增益范围足够，但推荐电源为 ±5 V；在“外部只给单路 5 V”的装置中会增加反相电源、噪声和调试成本，不作为第一选择。
- AD637：可作真有效值旁路复核，但低幅度时的带宽、建立时间和额外标定会增加风险；主结果使用数字真 RMS。
- 峰值检波、过零比较器：可作诊断或粗测，不作为最终幅值/基频依据。多谐波会改变过零点，峰值检波也难直接满足全频段 5 mV 绝对误差。
- 当前 AD9226 BLL 中固定的 `10.0/4096 V/LSB` 只能视为旧 demo 假设，不能视为模块实际量程证据。AD9226 芯片本身支持 1 Vpp/2 Vpp 输入跨度，SAD9226 模块是否带 ±5 V 前端必须实测确认。

## 6. 推荐硬件框图与参数目标

### 6.1 输入与前端

1. BNC 输入座。
2. 50 Ω/高阻可选终端，默认方案必须与现场信号源的负载设置一致；两种状态分别标定，避免“高阻读数翻倍”。
3. 小电容、低泄漏钳位保护和限流电阻。
4. 宽带缓冲。
5. 低通滤波：
   - 通带：10～500 kHz；
   - 500 kHz 的衰减可通过频响校准补偿，但必须稳定；
   - 1 MHz 起进入阻带；
   - 目标：模拟加数字合计在 1 MHz 不低于 50 dB；
   - 模拟部分需足以避免最坏输入下 ADC/放大器削顶，并抑制高频混叠。
6. 两档固定增益：
   - 低增益档覆盖 `250 mVpp + 200 mVpp` 最坏叠加；
   - 高增益档提高 50 mVpp 输入的码值利用率；
   - 具体倍数必须根据手上 SAD9226 模块的真实输入跨度决定，不能直接照搬旧代码。
7. AD9226 模块：
   - AVDD 由 5 V 供电；
   - 数字输出驱动电源必须确认是 3.0/3.3 V，不能把 5 V 并口直接送入不兼容的 H750 引脚；
   - 时钟、12 位数据线和地线应短、等长、就近去耦。

### 6.2 采样参数

主方案建议：

- AD9226 原始采样率：8.000 MS/s；
- 原始帧：16384 点；
- 原始捕获时间：2.048 ms；
- 数字低通后 4 倍抽取；
- 分析采样率：2.000 MS/s；
- 分析帧：4096 点；
- FFT 频率间隔：488.28125 Hz，满足 500 Hz 分辨率；
- 500 kHz 在原始数据中每周期约 16 点，在抽取数据中每周期约 4 点。

片内 ADC 备选建议：

- 优先选择直接 ADC 通道；
- 12 位、约 4.000 MS/s；
- 8192 点原始帧；
- 数字低通后 2 倍抽取为 2 MS/s、4096 点分析帧；
- FFT 频率间隔同样为 488.28125 Hz；
- 必须完成 ADC 线性校准、偏置/增益标定和 500 kHz 满幅实测。

## 7. 推荐算法流程

### 7.1 自动量程

1. 先采一小帧估计原始峰峰值和是否削顶。
2. 根据滤波后的有效信号幅度选择高/低增益档。
3. 切换后等待固定建立时间，再采正式帧。
4. 增益档增加滞回，避免连续跳档。

### 7.2 干扰抑制与抽取

1. 原始 8 MS/s 数据去直流和码值校正。
2. FIR 低通保留 0～500 kHz，强抑制 1 MHz 及以上。
3. 4 倍抽取到 2 MS/s。
4. 同时保留滤波前后峰值，检测前端削顶和滤波异常。

### 7.3 频率与谱线识别

1. 对 4096 点分析数据加窗并作正频率 FFT。
2. 搜索 10～500 kHz 的显著谱峰。
3. 取 2～3 根主谱线，通过整数倍关系搜索基频，不能直接选最大峰。
4. 对基频和各谐波频率做插值或最小二乘频率细化。
5. 频率内部目标误差不超过 250～500 Hz。

### 7.4 精确幅值、Upp 和真 RMS

1. 在 8 MS/s 滤波数据或同等高采样率数据上，按细化后的各频率作多正弦最小二乘拟合，得到每个分量的正弦/余弦系数；不只依赖抽取后每周期点数较少的数据。
2. 频谱幅值显示 `sqrt(a²+b²)`，即题目公式中的峰值幅度，单位 mV。
3. 用全部已识别分量重构一个基波周期：
   - `Upp = max(reconstructed)-min(reconstructed)`；
   - `Urms = sqrt(mean(reconstructed²))`；
   - 同时用 `sqrt(sum(Ak²/2))` 作零直流正交谐波的交叉校验。
4. 不直接用含噪声原始帧的单次最大/最小值作为最终 Upp。
5. 应用“增益档 × 频率”的幅值修正表，线性插值补偿前端幅频响应。

### 7.5 稳定波形和频谱显示

- 波形显示使用拟合/重构结果，固定从同一基波相位开始。
- 1 周期与 3 周期各生成与屏幕横向像素匹配的点数，不传输整帧原始数据。
- 频谱只显示正频率轴；图中保留主要离散谱线的相对频率位置和相对高度。
- 数值页同时显示：
  - Upp；
  - Urms；
  - f1；
  - 每个谱线的频率、谐波次数和峰值幅度；
  - 当前增益档、是否削顶、结果有效标志。
- “1周期/3周期”最好保留物理按键或明确的触摸按键，避免评测时对“键控”的解释产生争议。

## 8. 实施阶段与 go/no-go 条件

### 阶段 A：AD9226 高速采集限时验证

限时：1.5～2 小时。

按顺序验证：

1. 确认 SAD9226 模块供电、输出逻辑电平和真实输入跨度。
2. 观察 PA8/PA6 的 8 MHz 时钟频率、占空比和边沿。
3. 用静态输入验证零点、中点、正负方向和 12 位码序。
4. 采集 100 kHz、500 kHz 正弦，确认：
   - DMA 每帧完成；
   - 无 DCMI overrun；
   - 样本顺序连续；
   - 频率方向和幅值随输入正确变化。

go 条件：上述四项全部通过。
no-go 条件：到时仍存在随机码、丢样、码序不明或 500 kHz 幅值明显不稳定，立即转片内 ADC。

### 阶段 B：模拟前端最小闭环

1. 完成 BNC、终端、保护、缓冲和一个固定增益档。
2. 扫描 10、50、100、200、300、400、500 kHz 的增益。
3. 加入 1 MHz、200 mVpp 干扰，检查前端是否削顶。
4. 测量 1 MHz、2 MHz、5 MHz 抑制度。
5. 再加入第二增益档和自动量程。

阶段通过标准：

- 500 kHz 被测信号无明显失真和削顶；
- 50 mVpp 输入仍有足够码值跨度；
- 模拟与数字滤波合计对 1 MHz 干扰达到至少 50 dB；
- 增益档切换后能在 2 s 内稳定。

### 阶段 C：先完成第1、2项

1. 完成 500 Hz 分辨率频谱。
2. 完成 2～3 谱线识别和基频判定。
3. 完成谱线峰值幅度、Upp、真 Urms。
4. 完成稳定 1/3 周期波形和正频率谱显示。
5. 在无干扰条件下覆盖 10～500 kHz 和 50～250 mVpp。

### 阶段 D：完成第3项

1. 接入最终模拟低通。
2. 接入 FIR 低通和抽取。
3. 在 `ub + uJ` 条件下复测全部时域、频域指标。
4. 覆盖 1 MHz、1.5 MHz、2 MHz、5 MHz 干扰点，避免只在单点“碰巧通过”。

### 阶段 E：显示与整机

1. 确认串口屏实际尺寸不小于 6 英寸。
2. 评估 115200 波特率；若刷新超过预算，再提高到屏幕稳定支持的波特率。
3. 所有页面启动后 2 s 内出现有效结果。
4. 单路 5 V 输入供电，整机冷启动、重复上电和按键切换无死机。

### 阶段 F：校准与报告

建立两类校准：

- 每个增益档的零点和增益校准；
- 10～500 kHz 的幅频校准表。

设计报告同步保留：

- AD9226 与片内 ADC 的比较选择；
- 采样率、点数、500 Hz 分辨率计算；
- 低通滤波器设计和 1 MHz 抑制度；
- FFT、基频判定、多正弦拟合和真 RMS 公式；
- 完整测试表、误差统计和最坏点分析。

## 9. 验收矩阵

至少覆盖以下用例：

| 类别 | 测试点 |
| --- | --- |
| 幅值边界 | 50、100、250 mVpp |
| 频率边界 | 10、200、500 kHz，以及靠近边界的点 |
| 谐波组合 | 基波+二次；基波+三次；基波+三次+四次；基波不是最大谱线 |
| 相位组合 | 同相、反相附近、不同初相，覆盖高峰均比波形 |
| 干扰 | 200 mVpp，1、1.5、2、5 MHz |
| 显示 | 1周期、3周期、正频率谱、全部数值 |
| 时间 | 冷启动和每次按键切换均小于 2 s |
| 电源/接口 | 单路 5 V；BNC；50 Ω/高阻设置明确 |

正式题目判据：

- `|Upp_meas-Upp_ref| <= 5 mV`
- `|Urms_meas-Urms_ref| <= 5 mV`
- `|f1_meas-f1_ref| <= 1 kHz`
- 每个频谱分量 `|A_meas-A_ref| <= 5 mV`

内部放行目标：

- 幅值最坏误差不超过 3 mV；
- 基频最坏误差不超过 500 Hz；
- 连续 10 次结果无无效帧、无跳档、无明显显示抖动；
- 1 MHz 干扰加入前后，ub 的幅值变化不超过 3 mV。

## 10. 当前证据边界

- 已确认：题目 PDF 共 3 页，以上约束来自题面；现有工程包含 AD9226、片内 ADC、FFT、DMA/D-Cache 和串口屏模块。
- 已确认：现有 AD9226 软件为 1 MS/s、4096 点，算法只接受约 800～1200 点/周期，不能直接覆盖本题。
- 已确认：现有 AD9226 demo 只完成编译/链接验证。
- 尚未确认：SAD9226 模块真实模拟输入跨度、数字输出电平、8 MHz DCMI 稳定性、500 kHz 幅值精度。
- 尚未确认：现有串口屏尺寸是否满足 6 英寸、115200 刷新是否满足 2 s。
- 尚未确认：任何本题要求均未通过实物验收。

## 11. 参考器件资料

- [AD9226 官方数据手册](https://www.analog.com/media/en/technical-documentation/data-sheets/ad9226.pdf)
- [STM32H750xB 官方产品页](https://www.st.com/en/microcontrollers-microprocessors/stm32h750xb.html)
- [STM32H7 片内 ADC 应用笔记 AN5354](https://www.st.com/resource/en/application_note/an5354-getting-started-with-16bit-adc-on-stm32h7-mcus-stmicroelectronics.pdf)
- [MAX262 官方产品页](https://www.analog.com/en/products/max262.html)
- [AD603 官方产品页](https://www.analog.com/en/products/ad603.html)
- [ADS8688 官方数据手册](https://www.ti.com/lit/ds/symlink/ads8688.pdf)
